name: Build Packs (Nightly)

on:
  - push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: short-sha
        uses: benjlevesque/short-sha@v3.0

      - name: Install Packwiz
        uses: actionhippie/install-packwiz@v1

      - name: Build Client Pack
        run: packwiz curseforge export -o "extraordinary_energy_modern-nightly-${{ env.SHA }}.zip"

      - name: Build Server Pack
        run: packwiz curseforge export -s server -o "extraordinary_energy_modern-server-nightly-${{ env.SHA }}.zip"

      - name: Upload Client build result
        uses: actions/upload-artifact@v4
        with:
          name: extraordinary_energy_modern-nightly-${{ env.SHA }}
          path: extraordinary_energy_modern-nightly-${{ env.SHA }}.zip 
          retention-days: 7

      - name: Upload Server build result
        uses: actions/upload-artifact@v4
        with:
          name: extraordinary_energy_modern-server-nightly-${{ env.SHA }}
          path: extraordinary_energy_modern-server-nightly-${{ env.SHA }}.zip
          retention-days: 7