# ?展?板供?器?槽位容量的倍率。
# 基??36，??仍?示36格，倍率会增加??数/?容量。
# 建?范? 1-16
pageMultiplier: 1

# ?置AE?建合成???程中的 wait/notify 次数，提升?吐但会降低?度??性
craftingPauseThreshold: 100000

# 无?收?器最大?接距?（?位：方?）
# 从端与主端的直?距?需小于等于??才会建立?接
wirelessMaxRange: 256.0

# 是否允?无?收?器跨?度建立?接
# ??后，从端可?接到不同?度的主端（忽略距?限制）
wirelessCrossDimEnable: true

# 无?收?器待机能耗
# 无?收?器的基?待机能耗（AE/t），同?作用于普通与??无?收?器
wirelessTransceiverIdlePower: 100.0

# 智能倍增?是否??板供?器??分配
# ?多个供?器有相同?板?生效，??后?求会均分到所有可用供?器，???全部分配??一供?器
# 注意：所有相?供?器需??智能倍增，否?可能失效
providerRoundRobinEnable: true

# 全局智能倍增的最大倍率限制（0 表示不限制）
# 此限制???次?板?出的倍增上限，用于控制一次推送的最大?放?模
# ?先?低于?板自身的供?器限制
smartScalingMaxMultiplier: 0

# 是否?示?板??玩家
# ??后将在?板 HoverText 上添加?板的??玩家
showEncoderPatternPlayer: true

# ?板?端默?是否?示槽位
# 影??入界面?SlotsRow的默?可?性，?影?客?端?示
patternTerminalShowSlotsDefault: true

# ?体加速器能量消耗基??
entityTickerCost: 512

# 是否?先从磁?提取FE能量（?当Applied Flux模?存在?生效）
# ??后，将?先??从磁?提取FE能量；反之?先消耗AE网?中的能量
prioritizeDiskEnergy: true

# ?体加速器黑名?：匹配的方?将不会被加速。支持通配符/正?（例如：minecraft:*）
# 格式：全名或通配符/正?字符串，例如 'minecraft:chest'、'minecraft:*'、'modid:.*_fluid'
entityTickerBlackList:

# ?外消耗倍率配置：?某些方??置?外能量倍率，格式 'modid:blockid multiplier'，例如 'minecraft:chest 2x'
# 支持通配符/正?匹配（例如 'minecraft:* 2x' 会?整个命名空?生效）。
entityTickerMultipliers:
  - * 1000x

